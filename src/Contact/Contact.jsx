import React from 'react';
import PropTypes from 'prop-types';

const Contact = props => {
    return (
        <div>
            <h2>This is contact</h2>
        </div>
    );
};

Contact.propTypes = {
    
};

export default Contact;