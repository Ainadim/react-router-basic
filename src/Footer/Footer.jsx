import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <div>
            <p>This is footer</p>
        </div>
    );
};

Footer.propTypes = {
    
};

export default Footer;